<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#ff6ec7">
    <title>CandleVerse - Personalize sua Vela</title>
    <link rel="manifest" href="data:application/json;base64,">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-pink: #ff6ec7;
            --lilac: #b283ff;
            --soft-orange: #ffb380;
            --error-red: #ff4444;
            --text-dark: #333;
            --bg-light: #fff5f9;
            --shadow: 0 4px 12px rgba(255, 110, 199, 0.15);
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            background: linear-gradient(135deg, var(--bg-light) 0%, #fff 100%);
            color: var(--text-dark);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        header {
            text-align: center;
            margin-bottom: 40px;
        }

        h1 {
            font-size: clamp(2rem, 5vw, 3rem);
            background: linear-gradient(135deg, var(--primary-pink) 0%, var(--lilac) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 10px;
        }

        .subtitle {
            color: #666;
            font-size: 1.1rem;
        }

        .main-content {
            display: grid;
            grid-template-columns: 1fr;
            gap: 30px;
            margin-bottom: 30px;
        }

        @media (min-width: 768px) {
            .main-content {
                grid-template-columns: 1fr 1fr;
            }
        }

        .card {
            background: white;
            border-radius: 20px;
            padding: 30px;
            box-shadow: var(--shadow);
        }

        .section-title {
            font-size: 1.5rem;
            color: var(--primary-pink);
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: var(--text-dark);
        }

        select, input {
            width: 100%;
            padding: 12px 16px;
            border: 2px solid #e0e0e0;
            border-radius: 12px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: white;
        }

        select:focus, input:focus {
            outline: none;
            border-color: var(--primary-pink);
            box-shadow: 0 0 0 3px rgba(255, 110, 199, 0.1);
        }

        select.error, input.error {
            border-color: var(--error-red);
        }

        .error-message {
            color: var(--error-red);
            font-size: 0.875rem;
            margin-top: 5px;
            display: none;
        }

        .error-message.show {
            display: block;
        }

        .mockup-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 400px;
            position: relative;
        }

        .candle-mockup {
            position: relative;
            width: 200px;
            height: 300px;
            display: flex;
            flex-direction: column;
            align-items: center;
            filter: drop-shadow(0 10px 20px rgba(0,0,0,0.1));
        }

        .flame {
            width: 20px;
            height: 30px;
            background: linear-gradient(to top, var(--soft-orange), #ffeb3b, transparent);
            border-radius: 50% 50% 50% 50% / 60% 60% 40% 40%;
            animation: flicker 2s ease-in-out infinite;
            margin-bottom: 5px;
        }

        @keyframes flicker {
            0%, 100% { transform: scale(1) translateY(0); opacity: 1; }
            50% { transform: scale(1.05) translateY(-2px); opacity: 0.9; }
        }

        .wick {
            width: 3px;
            height: 15px;
            background: #333;
            margin-bottom: -5px;
        }

        .jar {
            width: 180px;
            height: 220px;
            background: var(--primary-pink);
            border-radius: 10px 10px 15px 15px;
            position: relative;
            opacity: 0.9;
            box-shadow: inset 0 -20px 30px rgba(0,0,0,0.1);
        }

        .jar::before {
            content: '';
            position: absolute;
            top: 10px;
            left: 10px;
            right: 10px;
            height: 30px;
            background: rgba(255,255,255,0.3);
            border-radius: 5px;
        }

        .jar-label {
            position: absolute;
            bottom: 20px;
            left: 20px;
            right: 20px;
            background: rgba(255,255,255,0.9);
            padding: 10px;
            border-radius: 8px;
            text-align: center;
            font-size: 0.875rem;
            color: var(--text-dark);
        }

        .specs {
            margin-top: 20px;
            padding: 15px;
            background: var(--bg-light);
            border-radius: 12px;
            font-size: 0.9rem;
        }

        .specs strong {
            color: var(--primary-pink);
        }

        .button-group {
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-top: 30px;
        }

        @media (min-width: 480px) {
            .button-group {
                flex-direction: row;
            }
        }

        button {
            flex: 1;
            padding: 14px 24px;
            border: none;
            border-radius: 12px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        button::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255,255,255,0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        button:hover::before {
            width: 300px;
            height: 300px;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary-pink) 0%, var(--lilac) 100%);
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(255, 110, 199, 0.4);
        }

        .btn-secondary {
            background: white;
            color: var(--primary-pink);
            border: 2px solid var(--primary-pink);
        }

        .btn-secondary:hover {
            background: var(--primary-pink);
            color: white;
        }

        .btn-tertiary {
            background: var(--soft-orange);
            color: white;
        }

        .btn-tertiary:hover {
            background: #ff9d5c;
            transform: translateY(-2px);
        }

        button:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }

        .loading {
            display: none;
            text-align: center;
            padding: 20px;
        }

        .loading.show {
            display: block;
        }

        .spinner {
            border: 3px solid #f3f3f3;
            border-top: 3px solid var(--primary-pink);
            border-radius: 50%;
            width: 40px;
            height: 40px;
            animation: spin 1s linear infinite;
            margin: 0 auto 10px;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .success-message {
            display: none;
            background: linear-gradient(135deg, #4caf50 0%, #45a049 100%);
            color: white;
            padding: 20px;
            border-radius: 12px;
            text-align: center;
            margin-top: 20px;
            animation: slideIn 0.5s ease;
        }

        .success-message.show {
            display: block;
        }

        @keyframes slideIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .firebase-warning {
            background: #fff3cd;
            border: 2px solid #ffc107;
            border-radius: 12px;
            padding: 15px;
            margin-bottom: 20px;
            font-size: 0.9rem;
        }

        .firebase-warning strong {
            color: #856404;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>🕯️ CandleVerse</h1>
            <p class="subtitle">Personalize sua vela dos sonhos</p>
        </header>

        <div class="firebase-warning" id="firebaseWarning">
            <strong>⚠️ Configuração necessária:</strong> Configure seu Firebase no código (busque por "FIREBASE_CONFIG" no HTML).
        </div>

        <div class="main-content">
            <!-- Personalização -->
            <div class="card">
                <h2 class="section-title">✨ Personalize</h2>
                
                <div class="form-group">
                    <label for="color">Cor da Vela</label>
                    <select id="color">
                        <option value="#ff6ec7">Rosa Vibrante</option>
                        <option value="#b283ff">Lilás Suave</option>
                        <option value="#ffb380">Laranja Pôr do Sol</option>
                        <option value="#80d4ff">Azul Céu</option>
                        <option value="#ff8080">Vermelho Paixão</option>
                        <option value="#a0e080">Verde Menta</option>
                    </select>
                </div>

                <div class="form-group">
                    <label for="aroma">Aroma</label>
                    <select id="aroma">
                        <option value="Lavanda">Lavanda Relaxante</option>
                        <option value="Baunilha">Baunilha Doce</option>
                        <option value="Citrus">Citrus Energizante</option>
                        <option value="Canela">Canela & Especiarias</option>
                        <option value="Oceano">Brisa do Oceano</option>
                        <option value="Rosa">Rosa Romântica</option>
                    </select>
                </div>

                <div class="form-group">
                    <label for="container">Recipiente</label>
                    <select id="container">
                        <option value="Vidro Clássico">Vidro Clássico</option>
                        <option value="Cerâmica Artesanal">Cerâmica Artesanal</option>
                        <option value="Lata Vintage">Lata Vintage</option>
                        <option value="Vidro Fosco">Vidro Fosco</option>
                    </select>
                </div>

                <div class="form-group">
                    <label for="size">Tamanho</label>
                    <select id="size">
                        <option value="Pequeno (100g)">Pequeno (100g)</option>
                        <option value="Médio (200g)">Médio (200g)</option>
                        <option value="Grande (350g)">Grande (350g)</option>
                        <option value="Extra Grande (500g)">Extra Grande (500g)</option>
                    </select>
                </div>
            </div>

            <!-- Mockup -->
            <div class="card">
                <h2 class="section-title">🎨 Pré-visualização</h2>
                <div class="mockup-container">
                    <div class="candle-mockup">
                        <div class="flame"></div>
                        <div class="wick"></div>
                        <div class="jar" id="jarPreview">
                            <div class="jar-label" id="jarLabel">Sua Vela Personalizada</div>
                        </div>
                    </div>
                    <div class="specs" id="specs">
                        <strong>Aroma:</strong> <span id="aromaSpec">Lavanda Relaxante</span><br>
                        <strong>Recipiente:</strong> <span id="containerSpec">Vidro Clássico</span><br>
                        <strong>Tamanho:</strong> <span id="sizeSpec">Pequeno (100g)</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Dados do usuário e ações -->
        <div class="card">
            <h2 class="section-title">💾 Salvar Design</h2>
            
            <div class="form-group">
                <label for="userName">Nome Completo *</label>
                <input type="text" id="userName" placeholder="Seu nome">
                <div class="error-message" id="userNameError">Por favor, insira seu nome</div>
            </div>

            <div class="form-group">
                <label for="userPhone">Telefone *</label>
                <input type="tel" id="userPhone" placeholder="(00) 00000-0000">
                <div class="error-message" id="userPhoneError">Insira um telefone válido (mínimo 10 dígitos)</div>
            </div>

            <div class="form-group">
                <label for="userEmail">E-mail *</label>
                <input type="email" id="userEmail" placeholder="seu@email.com">
                <div class="error-message" id="userEmailError">Insira um e-mail válido</div>
            </div>

            <div class="loading" id="loading">
                <div class="spinner"></div>
                <p>Salvando seu design...</p>
            </div>

            <div class="success-message" id="successMessage">
                ✅ Design salvo com sucesso! ID: <strong id="designId"></strong>
            </div>

            <div class="button-group">
                <button class="btn-primary" id="saveBtn" onclick="saveDesign()">💾 Salvar Design</button>
                <button class="btn-secondary" id="shareBtn" onclick="shareDesign()" disabled>📤 Compartilhar</button>
                <button class="btn-tertiary" onclick="resetDesign()">🔄 Nova Vela</button>
            </div>
        </div>
    </div>

    <!-- Firebase SDK -->
    <script type="module">
    import { initializeApp } from "https://www.gstatic.com/firebasejs/12.4.0/firebase-app.js";

        // ========================================
        // FIREBASE_CONFIG - CONFIGURE AQUI!
        // ========================================
        const firebaseConfig = {
    apiKey: "AIzaSyDAS6c3o0ozy87FYml06aA9ObTSFDZOoI4",
    authDomain: "candleversetest.firebaseapp.com",
    projectId: "candleversetest",
    storageBucket: "candleversetest.firebasestorage.app",
    messagingSenderId: "919330781977",
    appId: "1:919330781977:web:01d25dacc11a2b704fa388"
  };

        // Inicializar Firebase
        let app, db;
        try {
            app = initializeApp(firebaseConfig);
            db = getFirestore(app);
            document.getElementById('firebaseWarning').style.display = 'none';
            console.log('✅ Firebase inicializado com sucesso');
        } catch (error) {
            console.error('❌ Erro ao inicializar Firebase:', error);
        }

        // Tornar db global para as funções
        window.db = db;
        window.addDoc = addDoc;
        window.collection = collection;
    </script>

    <script>
        let savedDesignId = null;

        // Atualizar preview em tempo real
        document.getElementById('color').addEventListener('change', updatePreview);
        document.getElementById('aroma').addEventListener('change', updatePreview);
        document.getElementById('container').addEventListener('change', updatePreview);
        document.getElementById('size').addEventListener('change', updatePreview);

        function updatePreview() {
            const color = document.getElementById('color').value;
            const aroma = document.getElementById('aroma').value;
            const container = document.getElementById('container').value;
            const size = document.getElementById('size').value;

            document.getElementById('jarPreview').style.background = color;
            document.getElementById('aromaSpec').textContent = aroma;
            document.getElementById('containerSpec').textContent = container;
            document.getElementById('sizeSpec').textContent = size;
            document.getElementById('jarLabel').textContent = aroma;
        }

        // Validações
        function validateEmail(email) {
            const re = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            return re.test(email);
        }

        function validatePhone(phone) {
            const digits = phone.replace(/\D/g, '');
            return digits.length >= 10;
        }

        function showError(fieldId, show) {
            const field = document.getElementById(fieldId);
            const error = document.getElementById(fieldId + 'Error');
            
            if (show) {
                field.classList.add('error');
                error.classList.add('show');
            } else {
                field.classList.remove('error');
                error.classList.remove('show');
            }
        }

        // Salvar design
        async function saveDesign() {
            const userName = document.getElementById('userName').value.trim();
            const userPhone = document.getElementById('userPhone').value.trim();
            const userEmail = document.getElementById('userEmail').value.trim();

            let hasError = false;

            // Validar nome
            if (!userName) {
                showError('userName', true);
                hasError = true;
            } else {
                showError('userName', false);
            }

            // Validar telefone
            if (!validatePhone(userPhone)) {
                showError('userPhone', true);
                hasError = true;
            } else {
                showError('userPhone', false);
            }

            // Validar email
            if (!validateEmail(userEmail)) {
                showError('userEmail', true);
                hasError = true;
            } else {
                showError('userEmail', false);
            }

            if (hasError) {
                return;
            }

            if (!window.db) {
                alert('⚠️ Firebase não configurado! Configure o Firebase no código.');
                return;
            }

            // Mostrar loading
            document.getElementById('loading').classList.add('show');
            document.getElementById('saveBtn').disabled = true;

            try {
                const designData = {
                    userName,
                    userPhone,
                    userEmail,
                    color: document.getElementById('color').value,
                    colorName: document.getElementById('color').options[document.getElementById('color').selectedIndex].text,
                    aroma: document.getElementById('aroma').value,
                    container: document.getElementById('container').value,
                    size: document.getElementById('size').value,
                    createdAt: new Date().toISOString()
                };

                const docRef = await window.addDoc(window.collection(window.db, 'designs'), designData);
                savedDesignId = docRef.id;

                // Esconder loading
                document.getElementById('loading').classList.remove('show');
                
                // Mostrar sucesso
                document.getElementById('designId').textContent = savedDesignId;
                document.getElementById('successMessage').classList.add('show');
                
                // Habilitar botão compartilhar
                document.getElementById('shareBtn').disabled = false;

                console.log('✅ Design salvo com ID:', savedDesignId);
            } catch (error) {
                console.error('❌ Erro ao salvar:', error);
                document.getElementById('loading').classList.remove('show');
                document.getElementById('saveBtn').disabled = false;
                alert('❌ Erro ao salvar design: ' + error.message);
            }
        }

        // Compartilhar design
        function shareDesign() {
            if (!savedDesignId) {
                alert('⚠️ Salve o design primeiro!');
                return;
            }

            const designUrl = window.location.origin + window.location.pathname + '?design=' + savedDesignId;
            const userName = document.getElementById('userName').value;
            const aroma = document.getElementById('aroma').value;
            const colorName = document.getElementById('color').options[document.getElementById('color').selectedIndex].text;

            const subject = `Meu Design CandleVerse - ${aroma}`;
            const body = `Olá! 🕯️\n\nCriei uma vela personalizada no CandleVerse:\n\n✨ Aroma: ${aroma}\n🎨 Cor: ${colorName}\n👤 Criado por: ${userName}\n\n🔗 Veja o design: ${designUrl}\n\nCriado com CandleVerse`;

            // Opção 1: Email
            const mailtoUrl = `mailto:?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
            
            // Opção 2: WhatsApp
            const whatsappText = `Olá! 🕯️ Criei uma vela personalizada no CandleVerse!\n\n✨ ${aroma} | 🎨 ${colorName}\n\n🔗 ${designUrl}`;
            const whatsappUrl = `https://wa.me/?text=${encodeURIComponent(whatsappText)}`;

            // Mostrar opções
            if (confirm('📧 Clique OK para enviar por Email ou CANCELAR para WhatsApp')) {
                window.open(mailtoUrl, '_blank');
            } else {
                window.open(whatsappUrl, '_blank');
            }
        }

        // Resetar design
        function resetDesign() {
            if (confirm('🔄 Tem certeza que deseja criar uma nova vela? As informações não salvas serão perdidas.')) {
                // Resetar selects
                document.getElementById('color').selectedIndex = 0;
                document.getElementById('aroma').selectedIndex = 0;
                document.getElementById('container').selectedIndex = 0;
                document.getElementById('size').selectedIndex = 0;

                // Resetar inputs
                document.getElementById('userName').value = '';
                document.getElementById('userPhone').value = '';
                document.getElementById('userEmail').value = '';

                // Limpar erros
                showError('userName', false);
                showError('userPhone', false);
                showError('userEmail', false);

                // Esconder mensagem de sucesso
                document.getElementById('successMessage').classList.remove('show');
                
                // Desabilitar botão compartilhar
                document.getElementById('shareBtn').disabled = true;
                document.getElementById('saveBtn').disabled = false;

                // Resetar ID salvo
                savedDesignId = null;

                // Atualizar preview
                updatePreview();
            }
        }

        // Inicializar preview
        updatePreview();
    </script>
</body>
</html>
